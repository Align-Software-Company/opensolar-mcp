@AGENTS.md

# Claude Code

Use `AGENTS.md` as the canonical repository instructions.

Before making changes, read the relevant implementation and tests rather than relying on documentation alone. Do not publish packages, registry metadata, releases, or hosted services unless explicitly requested.
